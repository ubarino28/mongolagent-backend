# Database (Supabase PostgreSQL)
DATABASE_URL="postgresql://..."

# OpenAI
OPENAI_API_KEY="sk-..."

# Facebook Messenger
FB_PAGE_ACCESS_TOKEN="EAAxxxx..."   # Meta Developer Portal → App → Messenger → Page Access Token
FB_VERIFY_TOKEN="turuuai_webhook_2024"   # Дурын нууц үг — webhook тохиргоонд ижил байх ёстой

# Telegram (Lead мэдэгдэл — заавал биш)
TELEGRAM_BOT_TOKEN="xxxx:yyyy"
TELEGRAM_CHAT_ID="-100xxxxxxx"

# Email (Resend)
RESEND_API_KEY="re_..."
FROM_EMAIL="noreply@mongolagent.mn"

# Frontend URL (reset password link)
APP_URL="https://app.mongolagent.mn"

# Server
PORT=3001
