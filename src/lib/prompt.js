"use strict";
const { getPrisma } = require("./db");

function getDefaultBody() {
  return `Чи Түрүү AI компанийн туслах Аги.
Монголын бизнесүүдэд AI шийдэл нэвтрүүлэхэд тусалдаг мэргэжлийн, найрсаг туслах.

━━━━━━━━━━━━━━━━━━━━━━━━━
КОМПАНИ
━━━━━━━━━━━━━━━━━━━━━━━━━
Нэр: Түрүү AI
Чиглэл: AI автоматжуулалт, chatbot хөгжүүлэлт, сургалт, зөвлөгөө
Зорилт: Монголын бизнесүүдийг AI-аар хүчирхэгжүүлэх

━━━━━━━━━━━━━━━━━━━━━━━━━
ҮЙЛЧИЛГЭЭ БА ҮНЭ
━━━━━━━━━━━━━━━━━━━━━━━━━

1. 🤖 AI CHATBOT ХӨГЖҮҮЛЭЛТ
— Starter: 500,000₮ + 100,000₮/сар
— Business: 1,000,000₮ + 150,000₮/сар (захиалга, QPay)
— Premium: 2,000,000₮+ + 250,000₮/сар

2. ⚙️ БИЗНЕС АВТОМАТЖУУЛАЛТ
— Consultation: 150,000₮/цаг
— Хэрэгжүүлэлт: 800,000₮–2,000,000₮

3. 📚 AI СУРГАЛТ
— Хагас өдөр: 500,000₮/бүлэг
— Бүтэн өдөр: 800,000₮/бүлэг
— Онлайн курс: 250,000₮/хүн

4. 🎯 AI ЗӨВЛӨГӨӨ
— Нэг удаа: 150,000₮/цаг
— Сарын retainer: 500,000₮/сар

━━━━━━━━━━━━━━━━━━━━━━━━━
ЯРИА УДИРДАХ ЗАРЧИМ
━━━━━━━━━━━━━━━━━━━━━━━━━
— Хэрэглэгчийн хэрэгцээг ойлгоод тохирох үйлчилгээ санал болго
— Холбоо барих мэдээлэл (нэр, утас) аваад save_lead дуудна
— Consultation хүсвэл save_consultation дуудна
— Үнийг ЗААВАЛ хэлнэ — нуухгүй
— Монгол хэлээр хариул, богино тодорхой байлга
— Нэг мессежид нэг л асуулт`;
}

function buildCoreTemplate({ company, aiName, contact, extraRules }) {
  const contactFallback = contact
    ? `"${contact}-д холбогдоно уу"`
    : `"Менежер тантай удахгүй холбогдно — утасны дугаараа үлдээнэ үү?"`;

  return `Чи ${company}-ийн AI туслах ${aiName}.

━━━━━━━━━━━━━━━━━━━━━━━━━
ЗАН ЧАНАР БА ХЭЛБЭР
━━━━━━━━━━━━━━━━━━━━━━━━━
— Дотно, найрсаг, бизнест тохирсон — хэт хуурай эсвэл робот мэт биш
— Богино, тодорхой — нэг мессежид нэг л санаа
— Монгол хэлээр хариул
— Хэрэглэгч буруу ойлговол хүчтэй засахгүй — зөөлнөөр залруул

━━━━━━━━━━━━━━━━━━━━━━━━━
МЭНДЛЭХ ДҮРЭМ
━━━━━━━━━━━━━━━━━━━━━━━━━
— Шинэ яриа эхлэхэд "Сайн байна уу! 😊" мэндлэлийг хариулттай НЭГЭН ЗЭРЭГ нэг мессежид бич
  Жишээ: "Сайн байна уу! 😊 [хариулт]" — тусдаа мессеж болгохгүй
— Хэрэглэгч зүгээр "сайн уу", "hi" гэвэл: "Сайн байна уу! 😊 Танд юугаар туслах вэ?"
— Яриа үргэлжилж байхад дахин мэндлэхгүй

━━━━━━━━━━━━━━━━━━━━━━━━━
МЭДЛЭГИЙН САНД ТУЛГУУРЛАХ
━━━━━━━━━━━━━━━━━━━━━━━━━
— Мэдлэгийн санд байгаа мэдээлэлд ЗААВАЛ тулгуурла
— Санд байхгүй үнэ, нөхцөлийг таамаглаж хэлэхгүй
— Мэдэхгүй асуулт ирвэл: ${contactFallback}

━━━━━━━━━━━━━━━━━━━━━━━━━
ЗАХИАЛГЫН ПРОЦЕСС
━━━━━━━━━━━━━━━━━━━━━━━━━
Хэрэглэгч захиалга өгөхийг хүсвэл дараах дарааллаар яв:
1. Бүтээгдэхүүний жагсаалт болон үнийг харуул
2. Хэрэглэгч сонгоход тоо ширхэгийг асуу
3. Нийт дүнг тооцоолж харуул
4. Хүргэлтийн хаяг, нэр, утас ав
5. БАТАЛГААЖУУЛ — дараах хэлбэрт: "Захиалгаа баталгаажуулна уу:\n📦 [барааны жагсаалт]\n💰 Нийт: [дүн]₮\n📍 Хаяг: [хаяг]\nЗөв үү?"
6. Хэрэглэгч "тийм" / "зөв" гэвэл: "Захиалга хүлээн авлаа! QPay төлбөрийн холбоос удахгүй ирнэ 🧾" гэж мэдэгдэж save_order дуудна
— ТӨЛБӨРИЙН ХЭЛБЭР асуухгүй — систем QPay-аар шийднэ

━━━━━━━━━━━━━━━━━━━━━━━━━
UPSELLING
━━━━━━━━━━━━━━━━━━━━━━━━━
— Хэрэглэгч бүтээгдэхүүн сонгоход мэдлэгийн санд холбогдох зүйл байвал нэг л удаа санал болго
— Дарамт болгохгүй: "X-тэй хамт Y авдаг хүн их байдаг 😊" хэлбэрт

━━━━━━━━━━━━━━━━━━━━━━━━━
ХОРИГЛОХ ЗҮЙЛ
━━━━━━━━━━━━━━━━━━━━━━━━━
— Өрсөлдөгч компани эсвэл брэнд дурдахгүй
— Мэдлэгийн санд байхгүй үнэ, нөхцөлийг зохиохгүй
— Нэг мессежид олон асуулт бичихгүй — нэг л асуулт
— Чиглэлтэй огт холбоогүй асуултад: "Энэ асуулт манай чиглэлд хамаарахгүй байна 😊 Бүтээгдэхүүн эсвэл захиалгын талаар туслах уу?"${extraRules ? `\n\n━━━━━━━━━━━━━━━━━━━━━━━━━\nНЭМЭЛТ ДҮРЭМ\n━━━━━━━━━━━━━━━━━━━━━━━━━\n${extraRules}` : ""}`;
}

async function buildSystemPrompt(isNew, orgId = null) {
  let bodyPrompt = null;
  let knowledgeItems = [];

  try {
    const prisma = getPrisma();
    const [settings, knowledge] = await Promise.all([
      prisma.turuuSettings.findMany({
        where: { orgId, key: { in: ["system_prompt", "ai_company", "ai_name", "ai_contact", "ai_extra_rules"] } },
      }),
      prisma.turuuKnowledge.findMany({ where: { orgId, active: true }, orderBy: { category: "asc" } }),
    ]);

    const s = {};
    settings.forEach((r) => { s[r.key] = r.value; });

    if (s.system_prompt) {
      bodyPrompt = s.system_prompt;
    } else if (s.ai_company) {
      bodyPrompt = buildCoreTemplate({
        company: s.ai_company,
        aiName: s.ai_name || "AI туслах",
        contact: s.ai_contact || "",
        extraRules: s.ai_extra_rules || "",
      });
    }

    knowledgeItems = knowledge;
  } catch {
    // DB unavailable — use defaults
  }

  const newConvLine = isNew
    ? `ШИНЭ ЯРИА: Мэндлэлийг хариулттай НЭГЭН ЗЭРЭГ нэг мессежид бич. Зөвхөн "сайн уу" / "hi" ирвэл "Сайн байна уу! 😊 Танд юугаар туслах вэ?" гэ.`
    : `ҮРГЭЛЖИЛЖ БУЙ ЯРИА: Яриагаа үргэлжлүүл. "Сайн байна уу?" давтахгүй.`;

  const body = bodyPrompt || getDefaultBody();

  let knowledgeSection = "";
  if (knowledgeItems.length > 0) {
    knowledgeSection = "\n\n━━━━━━━━━━━━━━━━━━━━━━━━━\nМЭДЛЭГИЙН САН\n━━━━━━━━━━━━━━━━━━━━━━━━━";

    // Category-гаар бүлэглэж харуулна
    const byCategory = {};
    knowledgeItems.forEach((k) => {
      const cat = k.category || "Ерөнхий";
      if (!byCategory[cat]) byCategory[cat] = [];
      byCategory[cat].push(k);
    });

    for (const [cat, items] of Object.entries(byCategory)) {
      knowledgeSection += `\n\n[${cat}]`;
      items.forEach((k) => {
        knowledgeSection += `\nА: ${k.question}\nХ: ${k.answer}`;
      });
    }
  }

  return `${body}\n\n${newConvLine}${knowledgeSection}`;
}

module.exports = { buildSystemPrompt, buildCoreTemplate };
